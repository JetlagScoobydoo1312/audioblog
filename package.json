{
  "name": "audioblog",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute audioblog --local --file=schema.sql",
    "db:remote": "wrangler d1 execute audioblog --remote --file=schema.sql"
  },
  "devDependencies": { "wrangler": "^4.0.0" }
}
